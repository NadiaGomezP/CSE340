<% if (title) { %>
    <div class="container-formall">
        <h1 class="form-title"><%= title %></h1>
    <%- messages() %>
    </div>
    <% } else {
      res.redirect('/')
    } %>

<div id="links">
    <!-- Link to trigger the process to deliver the "add new classification" view -->
    <a  href="/inv/add-classification">Add New Classification</a>
</div>

<div id="links">
    <!-- Link to trigger the process to deliver the "add new inventory" view -->
    <a  href="/inv/add-inventory">Add New Inventory</a>
</div>